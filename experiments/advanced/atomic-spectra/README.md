# Integration Note
Standard experiment format using `sim.js`.
Ensure `atomic-spectra` is registered correctly in the generic array.
Chart.js is required for rendering the live spectrum intensity graph.
