# Integration Note
This experiment exposes a standard module satisfying the `sim.js` interface.
It is completely integrated with the generic `initExperimentPage()` architecture.
In `js/categories.js`, ensure `fluid-dynamics` is registered to the `advanced` category.
